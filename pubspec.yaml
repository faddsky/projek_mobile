name: projek_mobile
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  get_storage: ^2.1.1
  google_fonts: ^6.2.1
  crypto: ^3.0.3 # Untuk enkripsi SHA-256
  local_auth: ^2.1.6 # Untuk Biometric (Sidik Jari/Face ID)
  sensors_plus: ^5.0.1
  intl: ^0.20.2
  http: ^1.6.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.0.7
  image: ^4.1.3
  permission_handler: ^11.3.1
  tflite_flutter: ^0.12.0
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  geolocator: ^14.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  url_launcher: ^6.2.5
  google_generative_ai: ^0.4.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/model_ecostep_v3_fixed.tflite
    - assets/labels.txt
    - assets/images/
    - .env
